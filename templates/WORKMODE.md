# Active work mode: e2e

Valid values: `e2e` | `manual`

- `e2e` — auto-execute end-to-end (`e2e-delivery`)
- `manual` — plan/assist only until the user asks to implement

Change the title line to switch mode, or use `/set-mode` / chat (“mode manual”).
See `ai-agents-rogue/WORKMODES.md`.
