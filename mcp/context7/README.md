# MCP package: context7

Up-to-date library documentation via Context7 remote MCP.

## Wire

```powershell
.\ai-agents-rogue\scripts\install-mcp.ps1 -Target . -Mcp context7
```

Optional: set `CONTEXT7_API_KEY` and add headers in the host MCP config (see https://context7.com/dashboard).

## Upstream

[upstash/context7](https://github.com/upstash/context7). Catalog: Rogue Development.
