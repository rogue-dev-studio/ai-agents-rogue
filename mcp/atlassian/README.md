# MCP package: atlassian

Atlassian Rovo MCP (Jira/Confluence) over SSE. Auth via OAuth in the host when prompted.

```powershell
.\ai-agents-rogue\scripts\install-mcp.ps1 -Target . -Mcp atlassian
```

Catalog: Rogue Development.
