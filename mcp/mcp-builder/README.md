# MCP package: mcp-builder (partial)

Use `mcp/_template/` in this catalog to scaffold packages. Optionally install `@modelcontextprotocol/sdk` via npm for custom servers.

```powershell
npm install -g @modelcontextprotocol/sdk
```
