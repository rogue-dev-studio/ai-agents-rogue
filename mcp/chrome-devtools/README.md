# MCP package: chrome-devtools

```powershell
.\ai-agents-rogue\scripts\install-mcp.ps1 -Target . -Mcp chrome-devtools
```

Requires Node.js and a Chromium-based browser. Optional env: `CHROME_PATH`.

## Upstream

Runtime: `chrome-devtools-mcp` on npm. Catalog: Rogue Development.
