# MCP package: jupyter (partial)

```bash
pip install jupyter jupyterlab notebook ipykernel mcp pydantic
python -m ipykernel install --user --name python3
```

Add an MCP server entry after you have a Jupyter MCP implementation to point at.

Status: **partial** — no auto-wire fragment yet.
