# MCP package: jupyter (partial)

Status: **partial** — docs only.

## Prerequisites

1. Python 3.10+
2. Jupyter / JupyterLab

## Setup aplikasi

```bash
pip install jupyter jupyterlab notebook ipykernel mcp pydantic
python -m ipykernel install --user --name python3
jupyter lab
```

## Wire

Belum ada fragment. Setelah punya Jupyter MCP server, tambahkan entry ke config host secara manual.

Skill: `skills/jupyter-notebooks`.

## Smoke

Buka JupyterLab dan jalankan notebook uji.
